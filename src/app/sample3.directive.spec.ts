import { Sample3Directive } from './sample3.directive';

describe('Sample3Directive', () => {
  it('should create an instance', () => {
    const directive = new Sample3Directive();
    expect(directive).toBeTruthy();
  });
});
