import { Sample2Directive } from './sample2.directive';

describe('Sample2Directive', () => {
  it('should create an instance', () => {
    const directive = new Sample2Directive();
    expect(directive).toBeTruthy();
  });
});
